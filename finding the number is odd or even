//finding the number is whether it is a even or odd
#include <stdio.h>

int main()
{
    int a,b;
    printf("enter the number:\n");
    scanf("%d",&a);
    if(a%2==0){
        printf("%d is a even number",a);
    }
    else{
        printf("%d is a odd number",a);
    }
    

    return 0;
}
